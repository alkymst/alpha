import "./globals.css";
import { Analytics } from "@vercel/analytics/react";
import cx from "classnames";
import { sfPro, inter } from "./fonts";
import Nav from "@/components/layout/nav";
import Footer from "@/components/layout/footer";
import { Suspense } from "react";

export const metadata = {
  title: "FluxCTO - Unlimited Access to Elite CTOs - CTO Subscription Service",
  description:
    "FluxCTO connects companies with experienced technology leaders who provide strategic guidance, drive innovation, and oversee technology initiatives. With personalized consultations, innovation workshops, and technical leadership development, FluxCTO empowers businesses to scale and thrive in today's digital landscape. Discover how FluxCTO's CTO Subscription Service can revolutionize your approach to technology leadership.",
  metadataBase: new URL("https://fluxcto.com"),
  themeColor: "#FFF",
};

export default async function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={cx(sfPro.variable, inter.variable)}>
        <div className="fixed h-screen w-full bg-gradient-to-br from-indigo-50 via-white to-cyan-100" />
        <Suspense fallback="...">
          <Nav />
        </Suspense>
        <main className="flex min-h-screen w-full flex-col items-center justify-center py-32">
          {children}
        </main>
        <Footer />
        <Analytics />
      </body>
    </html>
  );
}
